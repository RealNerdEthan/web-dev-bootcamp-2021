<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit Product</h1>
    <form action="/products/<%= foundProduct._id %>?_method=PUT" method="POST">
        <label for="name">Product Name:</label>
        <input type="text" name="name" placeholder="text" id="name" value="<%= foundProduct.name %>">
        <label for="price">Unit Price:</label>
        <input type="price" name="price" placeholder="0.00" id="price" value="<%= foundProduct.price %>">
        <label for="category">Select Category:</label>
        <select name="category" id="category">
            <% for(let category of categories){ %>
                <option value="<%=category%>" <%=foundProduct.category===category ? 'selected' : '' %>><%=category%>
                </option>
                <% } %>
        </select>
        <button>Submit</button>
    </form>

    <a href="/products/<%= foundProduct._id %>">Cancel</a>
</body>

</html>